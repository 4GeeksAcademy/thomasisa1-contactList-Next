import React from 'react';

const Footer = () => {
    return (
        <footer>
            {/* Remove the message */}
            {/* <p>Footer Content</p> */}
        </footer>
    );
};

export default Footer;